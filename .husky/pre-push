echo "🔍 Running pre-push checks..."

# Run type checking
npm run typecheck || exit 1

# Run linting
npm run lint || exit 1

# Run tests (allow no tests)
npm run test || true

# Run build to ensure it works (skip env validation)
SKIP_ENV_VALIDATION=true npm run build || exit 1

echo "✅ All pre-push checks passed!"
